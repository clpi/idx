#include <algorithm>
#include <vector>
#include <iostream>
#include <thread>

template<typename V, typename E> class NetExecutive {

};

template<typename V, typename E> class Graph {
    uint64_t edgeCap;
    uint64_t nodeCap;
    std::uint64_t numVertices = 0;
    std::uint64_t numEdges = 0;

    std::vector<V* > *adjlist;

public:
    

    Graph withNodeCap(std::uint64_t nodesCap) {

    }


};
